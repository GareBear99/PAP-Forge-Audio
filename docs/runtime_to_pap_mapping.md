# Runtime to PAP mapping

The uploaded runtime contained the right governance ideas for PAP:

- event + receipt discipline -> checkpoint manifests and build receipts
- rollback control -> deterministic project restore
- approval/promote mindset -> future candidate promotion for validated plugin branches
- patch/edit flow -> future symbol-grounded mutation engine for plugin source files

This repo turns those ideas into a plugin-focused package instead of a generic runtime shell.
