# PAP Forge roadmap

## v0.3.0
- branch heads and checkpoint lineage
- artifact receipts for generated files
- build-plan emission under `.pap/builds/`
- declared container image and build hook
- mutate command for checkpoint-based iteration

## Next
- run JUCE/CMake inside the builder image
- symbol-grounded code mutation for processor/editor files
- DSP block library with realtime-safety contracts
- preview rendering and audio feature validation
- stable candidate promotion / branch merge policy
